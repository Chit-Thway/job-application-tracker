@model RegisterViewModel
@{
    ViewData["Title"] = "Create account";
    ViewData["Section"] = "register";
}

<section class="account-page">
    <div class="account-card">
        <p class="eyebrow">Invitation only</p>
        <h1>Create your account.</h1>
        <p class="account-lead">Use the private one-time code you received. You will verify your email before signing in.</p>

        <form method="post" asp-action="Register" class="account-form">
            <div asp-validation-summary="ModelOnly" class="validation-summary" role="alert"></div>

            <div class="field">
                <label asp-for="DisplayName"></label>
                <input asp-for="DisplayName" autocomplete="name" />
                <span asp-validation-for="DisplayName" class="field-error"></span>
            </div>

            <div class="field">
                <label asp-for="Email"></label>
                <input asp-for="Email" autocomplete="email" />
                <span asp-validation-for="Email" class="field-error"></span>
            </div>

            <div class="field">
                <label asp-for="Password"></label>
                <input asp-for="Password" autocomplete="new-password" />
                <span asp-validation-for="Password" class="field-error"></span>
            </div>

            <div class="field">
                <label asp-for="ConfirmPassword"></label>
                <input asp-for="ConfirmPassword" autocomplete="new-password" />
                <span asp-validation-for="ConfirmPassword" class="field-error"></span>
            </div>

            <div class="field">
                <label asp-for="InvitationCode"></label>
                <input asp-for="InvitationCode" autocomplete="off" spellcheck="false" />
                <span asp-validation-for="InvitationCode" class="field-error"></span>
            </div>

            <button class="button button-primary" type="submit">Create account</button>
        </form>

        <div class="account-links">
            <a asp-action="Login">Already have an account?</a>
        </div>

        <p class="account-note">Each invitation expires, can be revoked, and works only once.</p>
    </div>
</section>
